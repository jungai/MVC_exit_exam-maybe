<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta http-equiv="X-UA-Compatible" content="ie=edge" />
    <title>Mock MVC</title>
  </head>
  <style type="text/css">
    .container {
      display: flex;
      flex-direction: column;
      align-items: center;
    }
    #title {
      font-size: 3em;
      color: tomato;
      text-transform: capitalize;
    }
    .add {
      background-color: antiquewhite;
      text-align: center;
      padding: .4em .4em;
    }

  </style>
  <body>
    <div class="container">
      <h1 id="title">
        <%= msg %>
      </h1>
      <h3>
        User List
      </h3>
      <ul>
        <% for (var i = 0; i < result.length; i++) { %>
          <li>
              id: <%= result[0].id%> | name: <%= result[0].name %>
          </li>
        <% } %>
      </ul>
      <button>test</button>
      <div class="add">
        <h1>Add User</h1>
        <form class="form" method="post">
          <input placeholder="insert name" type="text" name="name"/>
          <button>👍🏼 add</button>
        </form>
      </div>
    </div>
  <script>
    /**
     * js code must be here
     * /
  </script>
  </body>
</html>
